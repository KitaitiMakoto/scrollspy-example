image: node

pages:
  cache:
    paths:
      - node_modules/
  script:
    - mkdir .public
    - npm install
    - cp * .public/
    - mv .public public
  artifacts:
    paths:
      - public
  only:
    - master
